#
# Constants
#

JOBS = []
<%=job_list %>

# Create IoT application
$app = Application.new("edge-device", JOBS)

loop {
  # run IoT jobs
  $app.run
  # Handling the idle state in main loop
  $app.manage_power
  sleep_ms(1)
}
